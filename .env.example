# Copy to .env for local (non-docker) run
# For Docker Compose, defaults also work.

# Port where the API/web server will listen
PORT=8000

# SQLite database file path
SQLITE_PATH=/data/app.db

# Auth (single user)
# Generate a password hash for your password (see README)
ADMIN_USER=admin
ADMIN_PASSWORD_HASH=

# Cookie/session secret (change in your .env)
SESSION_SECRET=change-me
