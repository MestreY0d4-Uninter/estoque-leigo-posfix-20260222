# Frontend (placeholder)

No V1, a interface é servida pelo backend (FastAPI + templates) para manter o setup simples.

Este diretório fica reservado para evoluir para um frontend separado (ex.: React/Vite) em versões futuras.
